platform :ios, '10.0'
use_frameworks!

target 'BitUncle' do
    pod 'Stanwood/Core'
    pod 'Firebase'
    pod 'Firebase/RemoteConfig'

    target 'BitUncleTests' do
        inherit! :search_paths
    end
end
